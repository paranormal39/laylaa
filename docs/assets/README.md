# README assets

Drop the images referenced by the root `README.md` here:

- `baner1.png` — top-of-README banner.
- `infograph.png` — XRPL vs Midnight NFTs comparison infographic.

Keep these exact filenames so the README image links resolve. If you rename them, update the `<img src>` paths in `README.md` to match.
